from Calc import *
if __name__=="__main__":
    # here we learn about modules. In modules we wil break different components and create separate file for them
    # so we have created a separate file Calc.py and we are importing it to use its functions
    c=add(3,5)
    print(c)